services:
  db:
    image: postgres:17
    environment:
      POSTGRES_USER: whendoist
      POSTGRES_PASSWORD: whendoist
      POSTGRES_DB: whendoist
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: