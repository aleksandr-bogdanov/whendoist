"""
End-to-end tests using Playwright.

These tests require:
1. A running Whendoist server (uvicorn app.main:app)
2. Playwright browsers installed (playwright install)

Run with: pytest -m e2e tests/e2e/
"""
