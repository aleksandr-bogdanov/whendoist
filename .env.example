# =============================================================================
# Whendoist Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values.
# Never commit .env to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# Database
# -----------------------------------------------------------------------------
# PostgreSQL connection URL with asyncpg driver.
# Format: postgresql+asyncpg://user:password@host:port/database
DATABASE_URL=postgresql+asyncpg://whendoist:whendoist@localhost:5432/whendoist

# Database connection pool settings (optional, defaults shown)
# DB_POOL_SIZE=5        # Number of persistent connections
# DB_MAX_OVERFLOW=10    # Max extra connections under load
# DB_POOL_RECYCLE=300   # Recycle connections after N seconds

# -----------------------------------------------------------------------------
# Application
# -----------------------------------------------------------------------------
# Secret key for session encryption and CSRF protection.
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=changeme-generate-a-secure-key

# Base URL of the application (used for OAuth redirect URIs).
# Development: http://localhost:8000
# Production:  https://your-domain.com
BASE_URL=http://localhost:8000

# -----------------------------------------------------------------------------
# Todoist OAuth
# -----------------------------------------------------------------------------
# Create an app at: https://developer.todoist.com/appconsole.html
# Set redirect URL to: {BASE_URL}/auth/todoist/callback
TODOIST_CLIENT_ID=
TODOIST_CLIENT_SECRET=

# -----------------------------------------------------------------------------
# Google OAuth
# -----------------------------------------------------------------------------
# Create credentials at: https://console.cloud.google.com/apis/credentials
# 1. Create OAuth 2.0 Client ID (Web application)
# 2. Add authorized redirect URI: {BASE_URL}/auth/google/callback
# 3. Enable Google Calendar API in your project
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
